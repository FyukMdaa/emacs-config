#+title: init.el
#+property: header-args:emacs-lisp :tangle init.el :mkdirp yes
#+auto_tangle: t

* setup.el のブートストラップ

twist.nix がすでにすべてのパッケージを Nix store 上にビルド・配置して
いるので、setup.el 標準の =:package= は何もしない no-op に置き換える。

#+begin_src emacs-lisp
;; -*- lexical-binding: t; -*-

(eval-and-compile
  (require 'setup))

(setup-define :package
  (lambda (_order) nil)
  :documentation "no-op: パッケージのインストールは twist.nix が担当する。"
  :repeatable t
  :shorthand #'cadr)
#+end_src

* Customize の保存先を変更する

twist.nix が作る =user-emacs-directory= は Nix store 上の読み取り専用
ディレクトリなので、=M-x customize= の保存先はデフォルトのままだと
書き込みに失敗する。

#+begin_src emacs-lisp
(setq custom-file
      (expand-file-name "custom.el"
                         (or (getenv "XDG_CONFIG_HOME") "~/.config/emacs-custom/")))
(make-directory (file-name-directory custom-file) t)
(when (file-exists-p custom-file)
  (load custom-file))
#+end_src

バックアップ・自動保存・ロックファイルも同様に、書き込み可能な場所に
まとめておく。

#+begin_src emacs-lisp
(let ((var-dir (expand-file-name "emacs/"
                                  (or (getenv "XDG_CACHE_HOME") "~/.cache/"))))
  (make-directory var-dir t)
  (setq backup-directory-alist `((".*" . ,var-dir))
        auto-save-file-name-transforms `((".*" ,var-dir t))
        lock-file-name-transforms `((".*" ,var-dir t))))
#+end_src

* 基本設定 (ビルトイン機能のみ)

#+begin_src emacs-lisp
(setq-default indent-tabs-mode nil
              tab-width 4)

(setq inhibit-startup-screen t
      initial-scratch-message nil
      ring-bell-function #'ignore
      use-short-answers t)

(column-number-mode 1)
(global-display-line-numbers-mode 1)

(keymap-global-set "C-x C-b" #'ibuffer)
#+end_src

最近使ったファイルの一覧、ミニバッファの履歴、カーソル位置を覚えて
おく。

#+begin_src emacs-lisp
(setup recentf
  (:require recentf)
  (recentf-mode 1))

(setup savehist
  (:require savehist)
  (savehist-mode 1))

(setup saveplace
  (:require saveplace)
  (save-place-mode 1))
#+end_src

#+begin_src emacs-lisp
(setup text-mode
  (:hook visual-line-mode))
#+end_src


#+begin_src emacs-lisp
(setup (:package which-key)
  (:require which-key)
  (which-key-mode 1))
#+end_src
